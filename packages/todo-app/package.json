{
  "name": "aws-workspaces-todo-app",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "rm -rf dist && node esbuild.config.js",
    "package": "zip -r dist/function.zip dist/*"
  }
}
